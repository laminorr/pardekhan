<?php

return [
    /*
    |--------------------------------------------------------------------------
    | آستانهٔ اثبات اجتماعی (Social proof threshold)
    |--------------------------------------------------------------------------
    |
    | حداقلِ تعدادِ کلِ رأی‌ها روی یک تخصیصِ هفتگی، پیش از آن‌که نتیجهٔ جمعی
    | (چند نفر می‌بینند / چند نفر نه) برای اعضا آشکار شود. عضو علاوه بر این
    | آستانه باید خودش هم رأی داده باشد تا نتیجه را ببیند.
    |
    */
    'social_proof_threshold' => 5,

    /*
    |--------------------------------------------------------------------------
    | انتخابِ خودکارِ فیلمِ هفته (Automatic selection)
    |--------------------------------------------------------------------------
    |
    | auto_enabled     : کلیدِ اصلیِ انتخابِ خودکار (هم برای دستورِ زمان‌بند و هم
    |                    برای fallbackِ تنبل در صفحهٔ عمومی). با false هیچ تخصیصِ
    |                    خودکاری ساخته نمی‌شود.
    | repeat_gap_weeks : فیلمی که در N هفتهٔ اخیر استفاده شده، دوباره واجدِ شرایطِ
    |                    استفاده نیست (برای جلوگیری از تکرارِ زودهنگام).
    | lru_window       : هنگام استفادهٔ مجدد، به‌جای همیشه انتخابِ قدیمی‌ترین، به‌طور
    |                    تصادفی از میان N فیلمِ کم‌استفاده‌شده‌ترینِ واجدِ شرایط یکی
    |                    انتخاب می‌شود (برای تنوع).
    |
    */
    'auto_enabled'     => true,
    'repeat_gap_weeks' => 4,
    'lru_window'       => 3,
];
